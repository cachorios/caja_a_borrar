<div class="navbar navbar-static-top navbar-inverse">
    <div class="navbar-inner">
        <div class="container">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>

            <a class="brand" href="{{ path('home_page') }}" style="padding-top: 5px; padding-bottom: 4px;">
                <img src="{{ asset("img/logo-posadas.png") }}" >

            </a>
            <span class="title_nav tagline">Sistema de Caja </span>

            <div class="nav-collapse collapse" id="main-menu">
                <div class="auth pull-right">
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <img class="avatar" src="{{ asset( usuario_imagenes ~ usuario.foto) | apply_filter('foto_login') }}">
                    <span class="name">{{ usuario }}</span><br/>
                    <span class="links">
                        <a href="{{ path("perfil") }}">Perfil</a>
                        <a href="{{ path("seg_logout") }}">Salir</a>
                     </span>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>
